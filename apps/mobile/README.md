# Mobile App

React Native (Bare) + TypeScript application.

## Setup

```bash
# From project root
npm install

# iOS
cd apps/mobile
npx pod-install
npm run ios

# Android
npm run android
```

## Features

- react-native-video for HLS playback
- AsyncStorage favorites persistence
- Native performance
